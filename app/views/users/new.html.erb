<%= form_for(@user) do |f| %>
<%= render partial: 'form', locals: { f: f } %>

<div class="actions">
<%= f.submit :value => 'Stwórz Konto' %><%= button_tag "Powrót",:type => 'button',:class => 'button',:onclick => "location.href = '#{websites_url}'" %>
</div>
<% end %>
